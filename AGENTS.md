# AGENTS

本仓库协作约束如下：

## 语言与文案

- 与用户沟通默认使用中文。
- 新增文档默认使用中文。
- 新增代码注释默认使用中文。
- 前端页面文案默认使用中文。

## 开发工作区

- `v2-bootstrap` 分支对应目录：
  - `/root/project/agent-platform/agent-dev/.worktrees/v2-bootstrap`
- 在实现 v2 相关功能时，默认在该 worktree 下工作。

## 注释要求

- 所有非生成代码函数都必须补函数级注释。
- 复杂函数需要补步骤注释，使用 `1. 2. 3.` 这种顺序说明关键流程。
- 简单函数只需简短函数级注释，不需要步骤注释。
- `sqlc` 等生成代码不手工补注释。

## 测试要求

- 改动行为时需要补对应单测或端到端测试。
- 每次交付前至少运行：
  - `cd backend && go test ./...`
  - `cd frontend && corepack pnpm typecheck`
  - `cd frontend && corepack pnpm test:e2e`

## 启动约定

- 优先保证前端通过 `80` 端口可访问。
- 前端默认走同源 `/api`，由 Vite 代理到后端 `8080`。
- 如果只开放 `80` 端口，优先确保前端页面与 API 代理都能通过 `80` 正常访问。

## 当前用户偏好

- 先 push 本地提交，再继续后续工作。
- 修改完成后，要明确说明：
  - 做了什么
  - 没做什么
  - 如何启动验证
